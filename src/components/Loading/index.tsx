import React from 'react';
import { Container, LoadingIndicator } from './styles';

export default function Loading() {
    return (
        <Container>
            <LoadingIndicator />
        </Container>
    );
}
